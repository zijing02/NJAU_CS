#include <bits/stdc++.h>
using namespace std;
// 位运算
// 先把第k位移到最低位，然后和1做与运算
// n >> k & 1
int main()
{
    int n, k;
    n = 10;
    for (int i = 3; i >= 0; i--)
        cout << (n >> i & 1);
    return 0;
}